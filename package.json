{
  
  "name": "devgya-fullstack-saas",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "cd frontend && npm install && npm run build && cd ../backend && pip3 install -r requirements.txt",
    "start": "node start.js",
    "dev:backend": "cd backend && .\\venv\\Scripts\\python.exe -m uvicorn main:app --reload --port 8000"
  }
}
